server:
  port: 8080

logging:
  level:
    root: info

spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/mock_transaction_api?serverTimezone=GMT
  h2:
    console:
      enabled: false