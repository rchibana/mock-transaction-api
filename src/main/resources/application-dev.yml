server:
  port: 8080

logging:
  level:
    root: info

spring:
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  datasource:
    username: developer
    password: developer
    url: jdbc:postgresql://localhost:5432/mock_transaction_api?serverTimezone=GMT
  h2:
    console:
      enabled: false